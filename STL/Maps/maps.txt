Maps stores key value pair
it basically creates a mappping it stores in sorted order
not is the continuous order

syntax: 
map<datatype, datatype> m;
how to store

m[key] = value;
m.insert({pair});


unordered Maps
no order of values 

multimaps

if you remove value part from maps it will become set --> collection of some elements
1.Normal set 
2.unordered set
3.Multisets