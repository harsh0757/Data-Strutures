#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str = "GeeksForGeeks";
    int length = 0;

    for (int i = 0; str[i] != '\0'; i++)
    {
        length++;
    }
    cout << length << endl;
}